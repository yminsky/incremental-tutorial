;; -*- scheme -*-

(jbuild_version 1)

(executables
 ((names (main))
  (libraries (core async incremental_kernel incr_dom expect_test_helpers))
  (preprocess (pps (ppx_jane)))
  (flags (:standard -short-paths))
  ))

