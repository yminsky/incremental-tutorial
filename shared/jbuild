;; -*- scheme -*-

(jbuild_version 1)

(library
 ((name tutorial_shared)
  (libraries
   (async 
    core
    expect_test_helpers
    incr_map
    incremental_kernel
    textutils 
    ))
  (preprocess (pps (ppx_jane)))
  (flags (:standard -short-paths))
  ))

