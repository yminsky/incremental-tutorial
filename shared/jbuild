;; -*- scheme -*-

(jbuild_version 1)

(library
 ((name tutorial_shared)
  (libraries
   (
    async 
    core
    incr_map
    incremental_kernel
    sexp_pretty
    textutils 
    ))
  (preprocess (pps (ppx_jane)))
  (flags (:standard -short-paths))
  ))

