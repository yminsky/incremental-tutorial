;; -*- scheme -*-

(jbuild_version 1)

(library
 ((name tutorial_shared)
  (libraries
   (
    async 
    core
    incr_map
    incremental_kernel
    incremental
    sexp_pretty
    ))
  (preprocess (pps (ppx_jane)))
  (flags (:standard -short-paths))
  ))

